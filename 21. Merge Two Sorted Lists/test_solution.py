from unittest import TestCase
import solution
import listNode

testFunc = solution.Solution().mergeTwoLists


class TestSolution(TestCase):
    def test(self):
        testCases = [
            [[1, 2, 4], [1, 3, 4], [1, 1, 2, 3, 4, 4]]
        ]
        for test in testCases:
            for i
            if testFunc(INSERT_ARGS_HERE) != test[INSERT_ANS_IDX_HERE]:
                self.fail()
